*/* nowerr.conf
#*/* parallel.conf

# DEFINE recently properly set with rsyslog
#app-admin/rsyslog rsyslog.conf

# parallel build seems broken
app-admin/qtpass noparallel.conf

# parallel build seems broken
net-ftp/vsftpd noparallel.conf

net-libs/h323plus noparallel.conf

# does not fix catalyst build
#media-gfx/blender python36.conf
